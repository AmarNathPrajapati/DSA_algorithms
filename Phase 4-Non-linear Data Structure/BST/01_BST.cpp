// The inorder traversal of the binary search is always sorted.
// left subtree have always lesser value than root node.
// right subtree have always greater value than root node.
// time complexity in insertion and deletion take log(n) 